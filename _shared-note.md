# 40 支 demo 的共用骨架（給後續維護看的）

每支 `indexN.html` 都是自我包含單檔（只吃 CDN），但共用同一套結構與 CSS 命名，
確保 40 支放在一起像同一個作品集，而不是拼湊：

```
.bar     頁首固定列：左「← 全部 40 種特效」回索引，右邊兩個標籤
         <i>特效：<b>效果名</b></i>  <i class="ind">產業：xxx</i>
hero     該支特效的主舞台（唯一會因 demo 而異的區塊）
section  服務項目 .grid > .card ×3~4
stats    數據列
cta      行動呼籲
footer   「效果原理」一句話技術說明
```

共用 CSS 變數：`--bg --fg --mut --acc --line --card`
每支只改這幾個變數的值來換色系，其餘結構樣式完全相同。

深色系為主，走淺色的：3（保養）、19（募資）、27（補習班）、28（會計）、
35（兒童牙醫）、40（連鎖餐飲）。

CDN：three@0.160.0 UMD、gsap@3.12.5 + ScrollTrigger。
